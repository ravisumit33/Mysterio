import React from 'react';
import Box from '@material-ui/core/Box';

class NavBar extends React.Component {
  render() {
    return (
      <Box>
        Work in progress for NavBar
      </Box>
    );
  }
}

export default NavBar;
