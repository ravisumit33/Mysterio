import { createContext } from 'react';

export const AlertContext = createContext({});
export const ProfileContext = createContext({});
export const UserContext = createContext({});
export const GlobalDialogContext = createContext({});
export const WaitScreenContext = createContext({});
export const HydrationContext = createContext({});
export const ChatRoomInfoContext = createContext({});
export const ChatRoomDataContext = createContext({});
export const ChatMessageContext = createContext({});
export const ChatPlayerContext = createContext({});
